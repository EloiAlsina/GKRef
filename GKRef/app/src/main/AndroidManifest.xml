<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cat.itb.gkref">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GKRef">
        <activity
            android:name=".Activities.LaunchActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar">
        </activity>

        <activity
            android:name=".Activities.TeamsFragment"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar">
        </activity>

        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar">
        </activity>

        <activity
            android:name="MenuFragment"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar"
            tools:ignore="Instantiatable">
        </activity>

        <activity
            android:name="HomeFragment"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar"
            tools:ignore="Instantiatable">
        </activity>

        <activity
            android:name="FinalActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar"
            tools:ignore="Instantiatable">
        </activity>

        <activity
            android:name=".FragmentRef"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar"
            tools:ignore="Instantiatable">
        </activity>

        <activity
            android:name="TransitionFragment"
            android:label="@string/app_name"
            android:theme="@style/Theme.GKRef.NoActionBar"
            tools:ignore="Instantiatable">
        </activity>
    </application>

</manifest>